/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BuscarPipe } from './buscar.pipe';

describe('Pipe: Buscar', () => {
  it('create an instance', () => {
    let pipe = new BuscarPipe();
    expect(pipe).toBeTruthy();
  });
});
